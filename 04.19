// 4. 인수의 생략과 초기값 -- 초기값 줄 때 주는놈 뒤쪽으로
// 5. 인수의 개수를 고정하지 않기 

// 1. 변수의 범위 
// 2. 전역 변수
// 3. 정적 변수 

// 1. 변수의 값 전달과 참조 전달 -- 시험에 나옴 
    <?php
		function oneUp(&$var){  //& 붙이기 (주소를 의미)
		$var +=1;
		}
		//실행한다.
		$num = 5;
		oneUp($num);
		echo $num;
		?>
    2.
    3. 가변 변수 
    예제코드2  -- 시험에 나옴
    4. 가변 함수 
    6. 익명 함수 (변수에 넣고 괄호를 꼭 써줘야 함)
    
    5장 문자열 
    1. 큰따옴표로 감싼 문자열 
    1. 형식화 해서 문자열을 표시하는 ehco,printf();,print_r,sprintf(),vprintf()
    2. 문자열에서 문자 추출하기 //substr
    1. 불필요한 공백이나 개행 제거하기 trim() ltrim, rtrim
      htmlspecialchars() -- 시험에 나옴 
    2. html태그제거하기 // strip_tags 
    3. URL 인코딩 
      rawurlencode() -- 중요
      urlencode() -- 중요
    2. 문자열로 형 변환해 비교하는 함수 
      strcmp  -- 시험에 나옴
      strcasecmp() -- 시험에 나옴
    2. 발견된 위치부터 그 이후의 모든 문자열을 추출하기 
      검색 mb_strstr() 
    3. 검색해 일치하면 바꾸기 
      대치 str_replace
    4. 검색 문자와 바꾼 무자를 배열로 지정하기 
      여러개의 검색 문자 바꾸기 str_ireplace()
      여러개의 검색 문자를 각각 다른 문자로 바꾸기 str_replace
    6장 배열 
    1. 인덱스 배열 // 대괄호 시작, 중괄호 시작, 소괄호(앞에 array를 써줘야함) 시작
                  // 출력 print_r() 또는 var_dump() 배열 다 찍고 싶을 때
    3. 연관 배열 // 화살표가 중요 
    4. 문자열로 배열 만들기 // elplode() 쉼표로 구분된 이름 목록으로 배열을 만든다 delimiter
    5. 배열을 문자열로 만들기 // implode
    1. 배열의 요소 삭제하기 // array_splice
    2. 배열의 맨 앞/뒤 꺼내기 // array_pop, array_push
    3. 배열의 요소를 치환/삽입하기 
    8장 폼 
    .
    
    
